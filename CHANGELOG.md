# Changelog

## 2026-05-02 — v0.4.2: docs + brand alignment with organic-social-dashboard
Pulled `BRAND.md` and `DESIGN_BRIEF.md` from the companion organic-social-dashboard's `docs/` into this repo's `docs/`, adapted for paid-ads scope (different IA, Meta + Google two-platform model, USD spend-native). Created `LEARNINGS.md` and `DECISIONS.md` at repo root with the 7 + 6 entries from this session — the silent-zero-rows trap, cross-platform parity rule, lookup-reader payload sizing, Tailwind opacity utility on flat-named colors, KpiCard backward-compat aliasing, the why-we-mirror-organic-social choice, the why-Data-Status-is-load-bearing choice, why we kept the join in TypeScript for v0.4 and what triggers a Python migration. `CLAUDE.md` updated to reference all four docs as authoritative.

## 2026-05-02 — v0.4.1: design system pass on /spend (organic-social parity)
Replaced /spend v0.4's inline-styled `<div className="rounded-xl ...">` blocks with proper `Card` / `SectionCard` / `PageHeader` / `KpiCard` primitives mirroring the organic-social-dashboard. New components: `Card.tsx` (Shikho v1.0 paper surface, rounded-3xl, ambient shadow, optional `kind` left-border for provenance), `PageHeader.tsx` (title + scrape timestamp from runStatus + rightSlot for date picker / compare toggle, stacks correctly on mobile). KpiCard upgraded with paper → indigo-50/40 gradient ground, responsive text-2xl sm:text-3xl with break-words leading-tight (no more 7-digit overflow at 360 px), new `delta` (signed %) + `deltaAbs` props using `ok` / `bad` semantic colors. SpendBucketBar restyled as a divide-y list inside SectionCard (no more self-styled card), with channel-coded 1×5px bar instead of dot, 9pt UPPERCASE channel chip with 15% alpha tint background, top-12 cap + "+ N smaller buckets" footer to prevent mobile-list drowning. Δ pills moved from arbitrary opacity utilities to `shikho-sunrise-50` / `shikho-coral-50` tints. /spend rebuild adds a Data Status SectionCard at the bottom showing per-channel row count + spend in window — when a channel returns 0 rows the card border turns coral and points to `/api/debug`, fixing the silent-zero-rows failure mode that prompted this pass. Cross-platform asymmetry note redesigned as calmer rounded-2xl sunrise-50 aside (was a yellow scream). Top campaigns table goes responsive: adsets/ads counts hide below sm and reappear under the campaign name as inline text, bid_strategy column hides below md. Build green at 194 KB First Load JS. Brand audit clean (zero slate/gray/zinc).

## 2026-05-02 — v0.4: /spend overview page (Meta + Google by objective / optimization / ad signal)
New route at `/spend`. Single page, mobile-first, answers: where is the money going, is the split right, what changed vs prior period. Reads `Raw_Insights` (existing), then joins to `Raw_AdSets` (Meta optimization_goal), `Raw_Ads` (Meta conversion_specs JSON, Google ad_type), `Raw_AdGroups` (Google ad_group type), `Raw_Campaigns` (bid_strategy / bidding_strategy_type) — all read-only, all tabs already populated by the pipelines. New `EnrichedInsight` shape extends `UnifiedInsight` with `optimization`, `ad_signal`, `campaign_bid_strategy`. New `SpendBucket` rollup shape is **never merged across platforms** — Meta and Google buckets stay separate even when labels collide, with an explicit yellow asymmetry note in the UI. New `SpendBucketBar` component renders dimension breakdowns as horizontal bars with optional Δ pills (sunrise = increase, magenta = decrease). `priorPeriod()` resolves the equal-length immediately-prior comparison window. Comparison is opt-in via `?compare=1`; KPIs and breakdowns get signed deltas when on. Build green at 194 KB First Load JS. CONTRACTS impact: zero — only reads columns the pipelines already write.

## 2026-04-29 — v0.3.1: revert UI strip, keep stats lib
Misread the user's intent. "No component" meant "no LLM/API-key analysis on the dashboard side" — not "no UI components." Restored every visual component (KpiCard, SpendChart, FunnelBar, ObjectiveTable, HierarchyExplorer, DateRangePicker, ChannelStatus, StalenessBanner) from v0.2. Kept `lib/stats.ts` (sum/mean/median/percentile/stddev/cv/gini/topShare/summarize) — it's still useful and will get integrated into the visual dashboard for distribution chips, forecast bands, anomaly indicators in subsequent passes. Architecture intent confirmed: heavy statistical computation belongs in Python at the pipeline layer (scipy/numpy/pandas writing derived columns to Sheets), dashboard is a thin TypeScript renderer.

## 2026-04-29 — v0.3: [reverted] pure-numbers view, no styled components
Stripped every decorative component for ~30 minutes. Reverted in v0.3.1 — see above.

## 2026-04-29 — v0.2: real data + USD + hierarchy explorer + date range
First production-data render. Three structural changes shipped together:

1. **Column-name fixes (data flow).** v0.1 mapper expected snake_case headers but both pipelines write display-friendly headers (`Spend (USD)`, `Date (BDT)`, `Actions (JSON)`, `Cost (USD)`, `Channel Type`). Updated `metaRowToInsight` and `googleRowToInsight` to use the exact strings — every column reference is now contract-verified against `<pipeline>/src/sheets.py::RAW_INSIGHTS_HEADERS`. Same fix applied to `Analysis_Log` reader (`Run At (BDT)`, `Fetch Status`).

2. **USD throughout.** Both platforms return spend in account currency natively (USD for Shikho). Renamed `fmtBDT` → `fmtUSD` (`$` prefix, K/M/B suffixes, no Cr/L). Updated every UI label and comment from BDT to USD. The hardcoded BDT discipline in CONTRACTS.md was wrong; will update there next.

3. **Hierarchy explorer + date range.** New `HierarchyExplorer` component shows spend / impressions / clicks / conv / CPA aggregated at three levels (Campaign / Ad Group / Ad) with platform tabs (All / Meta / Google) and an objective dropdown. New `DateRangePicker` is URL-state driven (`?days=N` or `?start&end`) so the server component re-renders with the right window — bookmarkable, sharable, and filterable from 7d to custom. Added `byCampaign / byAdGroup / byAd` aggregations + `distinctObjectives` to `lib/aggregate.ts`. Extended `UnifiedInsight` with `ad_group_id/name`, `ad_id/name`.

## 2026-04-21 — v0.1.1: auth + staleness banner + project rules
- Password gate via `DASHBOARD_PASSWORD` + signed cookie via `AUTH_SECRET` — middleware redirects every non-public route to `/login`. Mirrors the organic-social-dashboard pattern verbatim. Routes spared: `/login`, `/api/auth`, Next internals.
- `<StalenessBanner />` rendered above the page header. Per-channel — surfaces only when at least one expected channel hasn't reported in 26h+ (yellow) or 50h+ (red). Hidden when everything is fresh, so it stops being noise.
- `CLAUDE.md` added — mirrors organic-social-dashboard project rules: mobile checklist, brand v1.0 hard rules, 8-perspective QA gate, post-commit doc routing.
- `.gitignore` extended to catch `*creds*.json`, `service-account*.json`, `client_secret*.json` so an accidental local copy can never be committed.

## 2026-04-21 — v0.1 scaffold
First scaffold of the paid-ads dashboard. Next.js 14 + Tailwind 3.4 + Recharts. Mirrors `shikho-organic-social-analytics/organic-social-dashboard` architecture and brand system. Single overview page with: KPI strip (total / Meta / Google / combined spend, conversions, CPA, ROAS), daily-spend stacked chart, funnel-stage mix bar, top-objectives table. Reads `Raw_Insights` from both pipeline sheets server-side via `googleapis` with 10-min ISR. Funnel stage derived inline from objective via rule map (placeholder until Phase 2 classifier ships). Empty-states for Google channel until that pipeline runs.
